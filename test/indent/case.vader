Given elixir (case statements):
  case some_function do
  :ok ->
  :ok
  { :error, :message } ->
  { :error, :message }
  end
Do:
  gg=G
Expect:
  case some_function do
    :ok ->
      :ok
    { :error, :message } ->
      { :error, :message }
  end
